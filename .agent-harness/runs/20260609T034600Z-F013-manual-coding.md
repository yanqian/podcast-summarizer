# Run Record: F013 - upgrade local demo data to current segment model

## Summary

- Date: 2026-06-09
- Agent role: Manual Coding Agent fallback plus self-evaluation
- Feature: F013 Upgrade local demo data to current segment model
- Result: Completed

## Repository State

- Starting commit: b50ae93
- Ending commit: b50ae93
- Working tree status: modified harness state/spec files plus export handler/test; local ignored SQLite data updated

## Commands Run

```bash
./init.sh
GOCACHE="$PWD/backend/.gocache" go test ./tests/contract
sqlite3 backend/data/podcast.db ".backup 'backend/data/podcast.db.pre-f013-backup'"
sqlite3 backend/data/podcast.db "PRAGMA foreign_keys=ON; BEGIN; UPDATE episode SET has_transcript = 1, updated_at = CURRENT_TIMESTAMP WHERE id IN (SELECT DISTINCT podcast_id FROM transcript_paragraph); UPDATE podcast_source SET has_transcript = 1, updated_at = CURRENT_TIMESTAMP WHERE id IN (SELECT DISTINCT podcast_id FROM transcript_paragraph); INSERT OR IGNORE INTO transcript_segment (id, episode_id, order_index, start_seconds, text, created_at) SELECT tp.id, tp.podcast_id, tp.order_index, CAST(tp.timestamp_seconds AS REAL), tp.text, tp.created_at FROM transcript_paragraph tp; INSERT OR IGNORE INTO summary_segment (id, episode_id, order_index, text, created_at) SELECT sp.id, tp.podcast_id, tp.order_index, sp.summary_text, sp.created_at FROM summary_paragraph sp JOIN transcript_paragraph tp ON tp.id = sp.transcript_paragraph_id; INSERT OR IGNORE INTO transcript_summary_mapping (id, episode_id, summary_segment_id, transcript_segment_id, source_order, created_at) SELECT 'legacy-map-' || sp.id, tp.podcast_id, sp.id, tp.id, 1, sp.created_at FROM summary_paragraph sp JOIN transcript_paragraph tp ON tp.id = sp.transcript_paragraph_id; COMMIT;"
sqlite3 backend/data/podcast.db "SELECT 'transcript_segment', count(*) FROM transcript_segment UNION ALL SELECT 'summary_segment', count(*) FROM summary_segment UNION ALL SELECT 'mapping', count(*) FROM transcript_summary_mapping;"
curl -sS http://127.0.0.1:8080/api/podcasts/0b56aa83-6acf-4532-b98a-ba9813da0400
```

## Evidence

- Tests: `go test ./tests/contract` passed.
- Tests: final `./init.sh` passed, including harness checks, backend tests, frontend tests/build, and local backend smoke.
- Data upgrade: `backend/data/podcast.db` now has `transcript_segment=20`, `summary_segment=20`, and `transcript_summary_mapping=20`.
- External behavior verification: existing local demo detail response now reports `hasTranscript=true` and returns populated `transcriptSegments`, `summarySegments`, and `paragraphs`.
- Capability gaps: root `make work` was unavailable because this repository has no root `Makefile`; manual fallback was used per harness workflow.

## Failure Analysis

- Failure domain: none
- Failure summary: none
- Harness improvement: none
- Follow-up feature: none

## Files Changed

- `backend/data/podcast.db`
- `backend/data/podcast.db.pre-f013-backup`
- `backend/src/api/handlers/export.go`
- `backend/tests/contract/export_test.go`
- `specs/001-podcast-summary-ui/spec.md`
- `.agent-harness/feature_list.json`
- `.agent-harness/progress.md`

## Evaluator Result

```text
EVAL_PASS: F013
```

## Follow-Up

- Restart the already-running backend process to pick up the export handler code change.
