# Run Record: F015 - manual coding

## Summary

- Date: 2026-06-09T08:28:24Z
- Agent role: Coding Agent
- Feature: F015 Simplify frontend Admin and Demo pages
- Result: Implemented and ready for evaluator review; not marked done pending evaluator gating.

## Repository State

- Starting commit: 4a7dc4c F013-F014 Remove legacy SQLite schemas
- Ending commit: not committed
- Working tree status: dirty with F015 planning state, F015 implementation files, progress update, and this run evidence.
- Orchestrator/manual fallback: manual fallback was explicit in the Coding Agent prompt. Root `make work` is not available because the repository has no root `Makefile`; feature state remains incomplete pending evaluator pass.

## Commands Run

```bash
git log --oneline -20
./init.sh
(cd frontend && npm test)
(cd frontend && npm run lint)
(cd frontend && npm run build)
(cd frontend && npm run dev -- --host 127.0.0.1)
./init.sh
```

## Evidence

- Startup verification: root `./init.sh` passed before changes, including harness verification, backend tests, frontend tests/build, and deterministic local backend smoke.
- Implementation: `frontend/src/pages/App.tsx` now exposes Demo and Admin screens through local state and built-in tab navigation, with Demo as the default portfolio-facing screen and no new routing dependency.
- Implementation: `frontend/src/pages/PodcastView.tsx` remains the Demo viewer and renders current episode detail API transcript segments beside mapped summary segments, including loading, empty, failed, and unmapped segment states.
- Implementation: `frontend/src/pages/AdminSubmissionPage.tsx` continues to submit URLs, distinguish existing episodes from new jobs, show processing status and errors, and support manual refresh.
- Cleanup: frontend-only `streamClient`, `jobPolling`, `ExportControls`, `ProgressIndicator`, and `StatusBanner` paths were removed; `podcastClient` no longer exposes `/view` or paragraph compatibility data.
- Docs: root `README.md` and `frontend/README.md` now describe the HTTP Admin/Demo flow instead of the obsolete frontend SSE streaming/export flow.
- Tests: `frontend/tests/unit/App.test.tsx` covers default Demo screen and Admin navigation; existing Admin and Demo viewer tests cover submission, duplicate behavior, refresh/status display, errors, and segment-to-summary mapping from real-shaped fixtures.
- Verification: frontend `npm test`, `npm run lint`, and `npm run build` passed.
- Final verification: root `./init.sh` passed after changes, including harness checks, backend tests, frontend Vitest tests, frontend TypeScript/Vite build, and deterministic local backend smoke.

## Verification Notes

- Local Vite dev server startup failed with `listen EPERM: operation not permitted 127.0.0.1:5173`, matching the known sandbox listener limitation recorded for F010 and F011.
- Browser visual inspection could not run because no local frontend server could bind in this environment.
- The browser limitation did not weaken F015 acceptance verification: deterministic interaction tests exercise the two-screen UI and API-backed states, while lint and production TypeScript/Vite build passed.

## Failure Analysis

- Failure domain: none
- Failure summary: no product failure remains. Browser/dev-server inspection was unavailable due to sandbox environment limits, but required feature behavior is covered by deterministic frontend tests and build verification.
- Harness improvement: none required for F015. The project already has durable frontend test/build verification in `./init.sh`; local browser availability is useful visual QA, not a required project capability for this feature.
- Capability gaps: none. The dev-server listener limitation is an environment limitation for optional visual inspection, not a required durable capability for F015 acceptance.
- Follow-up feature: none.

## Files Changed

- frontend/src/pages/App.tsx
- frontend/src/pages/PodcastView.tsx
- frontend/src/services/podcastClient.ts
- frontend/src/components/UrlInput.tsx
- frontend/src/services/streamClient.ts
- frontend/src/services/jobPolling.ts
- frontend/src/components/ProgressIndicator.tsx
- frontend/src/components/ExportControls.tsx
- frontend/src/components/StatusBanner.tsx
- frontend/tests/unit/App.test.tsx
- frontend/tests/unit/PodcastView.test.tsx
- frontend/README.md
- README.md
- .agent-harness/progress.md
- .agent-harness/runs/20260609T082824Z-F015-manual-coding.md

## Evaluator Result

```text
PENDING_EVALUATOR: F015
```

## Follow-Up

- Run Evaluator Agent for F015 before marking the feature done.
